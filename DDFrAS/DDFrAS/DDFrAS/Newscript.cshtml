@using DDFrAS;
@using DDFrAS.DDFrAS;
@{
    Page.Title = "Nieuw Script";
    Layout = "_Layout.cshtml";
}

@{
    string ssh_template = "";
    using (var context = new DDFrASEntities())
    {
        ssh_template = context.SSH_COMMANDS.Find(4).Command_Temp;
    }

    if (IsPost)
    {
        ////request form data
        //string vlan = Request.Form["vlan"];
        //string manip = Request.Form["manip"];
        //string username = Request.Form["username"];
        //string passwordssh = Request.Form["passwordssh"];
        //string terminal = Request.Form["terminal"];

        ////push form data to database
        //Newvdbinputs.Newvars(vlan, manip, username, passwordssh, terminal);
    }
}

<div class="container text-left">
    <h2>Nieuw Script Maken</h2><br />
    <form method="post">
        <textarea style="width: 100%;height:100%">@ssh_template</textarea>
        <button type="submit">Maak nieuw script</button>
    </form>
</div>